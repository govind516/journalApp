import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { useNavigate } from "react-router-dom";
import { motion } from "framer-motion";
import { ChevronLeft, ChevronRight, Info } from "lucide-react";
import { Button } from "@/components/ui/button";
import { fetchCalendar } from "@/lib/normalize";

const monthNames = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];

export default function Calendar() {
  const [year, setYear] = useState(new Date().getFullYear());
  const navigate = useNavigate();
  const calendar = useQuery({ queryKey: ["calendar", year], queryFn: () => fetchCalendar(year) });
  const days = new Map((calendar.data?.days ?? []).map((day) => [day.date, day]));
  const todayIso = new Date().toISOString().slice(0, 10);
  const monthCells = monthNames.map((_, month) => Array.from({ length: new Date(year, month + 1, 0).getDate() }, (_, index) => { const day = `${year}-${String(month + 1).padStart(2, "0")}-${String(index + 1).padStart(2, "0")}`; return { number: index + 1, date: day, data: days.get(day), future: day > todayIso }; }));
  return <section className="mx-auto max-w-6xl px-5 py-8 sm:px-8 lg:px-12 lg:py-12" data-testid="calendar-page"><header className="flex flex-col justify-between gap-5 border-b border-[var(--line)] pb-8 sm:flex-row sm:items-end"><div><p className="eyebrow eyebrow-rule" data-testid="calendar-eyebrow">The shape of a year</p><h1 className="display-heading mt-3 text-balance text-[32px] leading-[1.08] sm:text-5xl" data-testid="calendar-heading">Your writing year</h1><p className="mt-3 text-sm text-[var(--muted-ink)]" data-testid="calendar-description">Every filled square is a day you chose to pay attention.</p></div><div className="flex items-center gap-2"><Button variant="outline" size="icon" onClick={() => setYear((value) => value - 1)} data-testid="calendar-previous-year-button" aria-label="Previous year"><ChevronLeft size={16} /></Button><span className="min-w-16 text-center font-serif text-xl tabular-nums" data-testid="calendar-year-label">{year}</span><Button variant="outline" size="icon" onClick={() => setYear((value) => value + 1)} data-testid="calendar-next-year-button" aria-label="Next year"><ChevronRight size={16} /></Button></div></header><div className="mt-8 rounded-[28px] border border-[var(--line)] bg-[var(--paper)] p-5 shadow-[var(--shadow-md)] sm:p-8" data-testid="calendar-heatmap-card"><div className="mb-7 flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-2 text-xs text-[var(--muted-ink)]" data-testid="calendar-legend"><span>Less</span>{[0, 1, 2, 3, 4].map((value) => <span key={value} className={`heat-cell heat-${value}`} data-testid={`calendar-legend-cell-${value}`} />)}<span>More</span></div><span className="text-xs text-[var(--muted-ink)]" data-testid="calendar-written-count">{calendar.data?.days.length ?? 0} written days</span></div><div key={year} className="grid grid-cols-2 gap-5 md:grid-cols-4 lg:grid-cols-6" data-testid="calendar-month-grid">{monthCells.map((cells, month) => <div key={month} data-testid={`calendar-month-${month + 1}`}><p className="mb-3 text-xs font-semibold text-[var(--muted-ink)]">{monthNames[month]}</p><div className="grid grid-cols-7 gap-1 sm:gap-1.5">{cells.map((cell, index) => <motion.button key={cell.date} initial={{ opacity: 0, scale: 0.6 }} animate={{ opacity: 1, scale: 1 }} transition={{ delay: Math.min(0.7, month * 0.04 + index * 0.007), type: "spring", stiffness: 420, damping: 26 }} disabled={cell.future && !cell.data} title={cell.data ? `${cell.date}: ${cell.data.words} words` : cell.future ? `${cell.date}: not yet` : `${cell.date}: Write a backfill`} onClick={() => cell.data ? navigate(`/app/entry/${cell.data.entry_id}`) : !cell.future && navigate(`/app/today?date=${cell.date}`)} className={`heat-cell heat-${cell.data ? Math.min(4, Math.max(1, Math.ceil(cell.data.words / 80))) : 0} ${cell.future && !cell.data ? "opacity-40" : ""}`} data-testid={`calendar-day-${cell.date}`} aria-label={cell.data ? `Open entry for ${cell.date}` : cell.future ? `${cell.date} is in the future` : `Write a backfill for ${cell.date}`}>
</motion.button>)}</div></div>)}</div></div><div className="mt-5 flex items-start gap-3 rounded-2xl bg-[var(--sand)] px-5 py-4 text-sm leading-6 text-[var(--muted-ink)]" data-testid="calendar-backfill-note"><Info size={17} className="mt-0.5 shrink-0 text-[var(--terracotta)]" /><p data-testid="calendar-backfill-copy"><strong className="text-[var(--ink-soft)]">Missed a day?</strong> Tap an empty square to add a backfilled memory. It will stay part of your story without changing your daily rhythm.</p></div></section>;
}

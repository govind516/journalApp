# Frontend env (Vite build-time, VITE_ prefix required).
# Dev default: empty -> vite.config.ts proxies /api to http://127.0.0.1:8000.
# Prod direct-backend build: set to backend origin, e.g. https://api.example.com
VITE_API_BASE_URL=

# Informational only — the app reads process env, not this file.
# Names match backend/src/main/resources/application.properties placeholders.
DB_HOST=localhost
DB_NAME=journaldb
DB_USER=journal
DB_PASSWORD=journal
# Space-separated allowed frontend origins (maps to app.cors.origins).
# APP_CORS_ORIGINS=http://localhost:5173
# Journal intelligence provider for MVP (local only, no external AI).
# APP_AI_PROVIDER=local
